import React from 'react';
import createReactClass from 'create-react-class';

const TransactionSummary = createReactClass({
  render() {
    return (
      <div></div>
    );
  }
});

export default TransactionSummary;
